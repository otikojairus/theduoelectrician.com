# The Duo Electrician Route Notes

Dynamic pSEO pages live in `app/[slug]/page.tsx`; service pillars live in `app/services/[serviceSlug]/page.tsx`. Keep one H1 per page, FAQ schema where FAQs render, and internal links back to pillar pages.
