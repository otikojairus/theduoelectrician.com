# The Duo Electrician Working Guide

This is a programmatic SEO Next.js site for electrician services. Use `lib/electrician-data.ts` as the source of truth and keep generated pages linked through service pillars, city pages, related pages, sitemap, and the service directory. Use the `tde-*` design system in `app/globals.css`.
